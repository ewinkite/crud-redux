import { css } from "@emotion/react";
export const Container = css`
  width: 47.25rem;
  height: auto;
  background-color: var(--color-white);
  margin: 1.5rem auto;
  padding: 1.56rem;
  border-radius: 0.5rem;
`;

export const input = css`
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 0.8rem;
`;

export const markArea = css`
  width: auto;
  margin-top: 1rem;
`;

export const mainImgInput = css`
  display: none;
`;

export const mainImgBtn = css`
  width: 5em;
  height: 1.625rem;
  border-radius: 0.3125rem;
  border: 1px solid var(--color-main);
  background: var(--color-white);
  padding: 0.25rem;
  text-align: center;
  color: var(--color-main);
`;

export const mainImg = css`
  display: flex;
  align-items: center;
  gap: 0.7rem;
`;

export const mainImgWrap = css`
  display: flex;
  align-items: center;
  gap: 1rem;
`;
